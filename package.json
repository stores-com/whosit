{
    "description": "A personal name parser for Node.js: http://en.wikipedia.org/wiki/Personal_name",
    "devDependencies": {
        "@eslint/js": "*",
        "globals": "*"
    },
    "keywords": [
        "name",
        "parser"
    ],
    "main": "./lib",
    "name": "whosit",
    "repository": "https://github.com/stores-com/whosit",
    "scripts": {
        "coveralls": "nyc node --test --test-concurrency=true --test-reporter=spec && nyc report --reporter=text-lcov | coveralls",
        "test": "node --test --test-concurrency=true --test-reporter=spec"
    },
    "version": "1.3.1"
}
